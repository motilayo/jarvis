apiVersion: jarvis.io/v1
kind: Command
metadata:
  labels:
    app.kubernetes.io/name: controller
    app.kubernetes.io/managed-by: kustomize
  name: command-sample
  namespace: jarvis
spec:
  command: curl -k google.com
  selector:
    nodeSelectorTerms:
      - matchExpressions:
          - key: node-role.kubernetes.io/control-plane
            operator: DoesNotExist
