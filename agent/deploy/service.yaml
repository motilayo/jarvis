apiVersion: v1
kind: Service
metadata:
  name: jarvis-agent
  namespace: jarvis
  labels:
    app: jarvis-agent
spec:
  clusterIP: None
  selector:
    app: jarvis-agent
  ports:
    - name: grpc
      port: 50051
      targetPort: 50051
      protocol: TCP
